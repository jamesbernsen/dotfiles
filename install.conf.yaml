- clean: ['~']

- defaults:
    link:
      create: true
      relink: false
      force: false

- link:
    ~/.bash_profile:
        path: runcom/bash_profile
    ~/.bashrc:
        path: runcom/bashrc
    ~/.aws/config:
        path: aws/config
    ~/.gitconfig:
        path: git/gitconfig

