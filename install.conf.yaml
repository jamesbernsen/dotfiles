- clean: ['~']

- defaults:
    link:
      create: false
      relink: false
      force: false

- link:
    ~/.bash_profile:
        path: runcom/bash_profile
    ~/.bashrc:
        path: runcom/bashrc

